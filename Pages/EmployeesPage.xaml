<Page x:Class="Vet.Pages.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Vet.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1600"
      Title="Сотрудники" Loaded="Page_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="7.5*"/>
            <RowDefinition Height="1.5*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="left" Margin="0 0 150 0" Orientation="Horizontal">
<<<<<<< HEAD
            <TextBlock Text="Список сотрудников" Margin="5" Style="{StaticResource tbTitlePage}"/>
=======
            <TextBlock Text="Сотрудники" Margin="5" Style="{StaticResource tbTitlePage}"/>
>>>>>>> parent of a57e67c (medcards доделать добавить-удалить-редактировать)
        </StackPanel>
        <WrapPanel Grid.Row="0" HorizontalAlignment="right" Margin="0 0 150 0">
            <Grid>
                <TextBox x:Name="Search" Style="{StaticResource tbAuthWindow}" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" VerticalAlignment="Top" Width="250">
                    <TextBox.Effect>
                        <DropShadowEffect BlurRadius="4"  Direction="270" ShadowDepth="3" Color="LightGray"/>
                    </TextBox.Effect>
                </TextBox>
                <TextBlock IsHitTestVisible="False" Text="Поиск в реальном времени..." FontSize="14" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0 5 0 0">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=Search}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </WrapPanel>
<<<<<<< HEAD
        <WrapPanel HorizontalAlignment="Center" Grid.Row="2">
            <Button x:Name="btnAddEmployeeWindow" Style="{StaticResource btnAllPandW}" Content="Добавить запись" Click="btnAddEmployeeWindow_Click"  Margin="0 10 15 0">
=======
        <StackPanel HorizontalAlignment="Center" Grid.Row="2" Orientation="Horizontal">
            <Button x:Name="btnGoBack" Style="{StaticResource btnAllPandW}" Content="Назад" Click="btnGoBack_Click">
>>>>>>> parent of a57e67c (medcards доделать добавить-удалить-редактировать)
                <Button.Effect>
                    <DropShadowEffect BlurRadius="4"  Direction="270" ShadowDepth="3" Color="LightGray"/>
                </Button.Effect>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
<<<<<<< HEAD
            <Button x:Name="btnDelete" Style="{StaticResource btnAllPandW}" Content="Удалить запись" Click="btnDelete_Click" Margin="0 10 15 0">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="4" Direction="270" ShadowDepth="3" Color="LightGray"/>
                </Button.Effect>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button x:Name="btnSave" Style="{StaticResource btnAllPandW}" Content="Сохранить" Click="btnSave_Click" Margin="0 10 15 0">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="4" Direction="270" ShadowDepth="3" Color="LightGray"/>
                </Button.Effect>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button x:Name="btnGoBack" Style="{StaticResource btnAllPandW}" Content="Главное меню" Click="btnGoBack_Click" Margin="0 10 0 0">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="4" Direction="270" ShadowDepth="3" Color="LightGray"/>
                </Button.Effect>
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </WrapPanel>
        <DataGrid x:Name="EmployeesGrid" ItemsSource="{Binding Employee}" AutoGenerateColumns="False" Grid.Row="1" HorizontalAlignment="Stretch" IsReadOnly="False">
=======
        </StackPanel>
        <DataGrid x:Name="EmployeesGrid" ItemsSource="{Binding Employees}" AutoGenerateColumns="False" Grid.Row="1" HorizontalAlignment="Stretch" IsReadOnly="True" CanUserAddRows="False">
>>>>>>> parent of a57e67c (medcards доделать добавить-удалить-редактировать)
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер"          Binding="{Binding IDEmployee}"/>
                <DataGridTextColumn Header="Фамилия"        Binding="{Binding Surname}"/>
                <DataGridTextColumn Header="Имя"            Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Отчество"       Binding="{Binding FatherName}"/>
                <DataGridTextColumn Header="Номер телефона" Binding="{Binding Phone}"/>
                <DataGridTextColumn Header="Серия паспорта" Binding="{Binding PassSerial}"/>
                <DataGridTextColumn Header="Номер паспорта" Binding="{Binding PassNumber}"/>
<<<<<<< HEAD
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}"/>
                <DataGridTextColumn Header="Удален" Binding="{Binding IsDeleted}"/>
=======
                <DataGridTextColumn Header="Адрес"          Binding="{Binding Address}"/>
>>>>>>> parent of a57e67c (medcards доделать добавить-удалить-редактировать)
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>